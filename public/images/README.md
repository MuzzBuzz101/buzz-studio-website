Curated, compressed, web-ready photography lives here — this is what actually
ships with the site. Originals are kept at full resolution in `raw-photos/`
at the project root (git-ignored) in case you want different picks later.

- `projects/<slug>/` — cover.jpg + gallery (01.jpg, 02.jpg, ...) per case study
- `stills/<category>/` — the standalone Motion vs. Stills grid photos

`raccoon-restaurant` and `keyvant-real-estate` don't have photos yet — send
some over (see `raw-photos/README.md`) and I'll pick the best and add them.
